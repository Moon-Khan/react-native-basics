/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PrintDetail from './print_detail_form';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PrintDetail);
